# Solo para el modo desatendido (npm run agent). La aplicación de escritorio
# guarda las claves cifradas con el almacén del sistema operativo y NO usa
# este archivo. Nunca subas un .env real al repositorio.
TMDB_API_KEY=
OMDB_API_KEY=
